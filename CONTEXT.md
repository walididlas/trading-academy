# Trading Academy — Full Context Document

> Last updated: 2026-04-18  
> This file is the single source of truth for the entire project. Read this before touching any code.

---

## 1. What This Is

A personal, full-stack trading education platform built for one user. It teaches the ICC Kill Zone methodology from zero to execution, connects live to TradingView Desktop, scans markets in real time, and acts as an always-on AI trading assistant.

**Not a SaaS. Not multi-user. Built for one person — the owner.**

---

## 2. Tech Stack

### Frontend
| Thing | What |
|---|---|
| Framework | React 18 + Vite 5.4.21 |
| Routing | React Router v6 |
| Fonts | Inter (UI) + JetBrains Mono (numbers/code) via Google Fonts |
| State | `useState` + `useEffect` + Context API (no Redux) |
| Persistence | `localStorage` for journal, progress, balance, price alerts |
| Dev server | `http://localhost:5173` |
| Proxy | `/api` → `localhost:8000`, `/ws` → `ws://localhost:8000` |

### Backend
| Thing | What |
|---|---|
| Framework | FastAPI + Uvicorn |
| WebSocket | Native FastAPI WebSocket (no socket.io) |
| AI | Anthropic `claude-sonnet-4-6` via `anthropic` SDK |
| TradingView | CDP port 9222 via `aiohttp` WebSocket |
| HTTP client | `httpx` (async) |
| Port | `http://localhost:8000` |

### TradingView Connection
- TradingView Desktop (Electron) exposes CDP on port 9222
- Same connection used by the `tradingview-mcp` server (separate process)
- Backend reads OHLCV bars, price quotes via JS evaluation in the TV tab
- **Alternative path**: MCP tools can POST OHLCV data directly to `/api/feed` — scanner uses this cache first

---

## 3. File Structure

```
trading-academy/
├── start.sh                      # One-command startup
├── CONTEXT.md                    # This file
│
├── frontend/
│   ├── index.html
│   ├── vite.config.js
│   ├── vercel.json               # Vercel SPA routing + build config (at repo root)
│   ├── public/
│   │   ├── sw.js                 # Service worker (future Web Push)
│   │   ├── manifest.json         # PWA manifest (name, icons, shortcuts, display:standalone)
│   │   ├── icon-192.png          # PWA icon — candlestick on dark bg (generated by Python)
│   │   └── icon-512.png          # PWA icon large
│   └── src/
│       ├── main.jsx
│       ├── App.jsx               # All routes defined here
│       ├── index.css             # Full design system (tokens, components, animations)
│       ├── config.js             # Central API_BASE + WS_BASE (reads VITE_API_URL env var)
│       ├── data/
│       │   └── curriculum.js     # 9 modules, 20 lessons, all content + quizzes
│       ├── hooks/
│       │   ├── useProgress.js    # localStorage: XP, streak, completions, quiz scores
│       │   ├── useAccount.js     # localStorage: balance, riskPct, calcLots()
│       │   └── useCalendar.js    # Polls /api/calendar every 60s, getNewsRiskForPair()
│       ├── contexts/
│       │   └── AlertContext.jsx  # Single WS connection, shared signals/news/toasts/KZ state
│       ├── components/
│       │   ├── Layout/Sidebar.jsx
│       │   ├── Diagrams/index.jsx     # 8 SVG diagrams: candlestick, market-structure,
│       │   │                          #   bos-choch, liquidity, order-block, fvg,
│       │   │                          #   premium-discount, icc
│       │   ├── SetupDiagram.jsx       # Trade diagram SVG (entry/SL/TP levels)
│       │   ├── NewsShield.jsx         # Green/yellow/red news risk badge for signal cards
│       │   ├── NewsFeed.jsx           # Scrollable live news feed with impact badges
│       │   ├── NewsTicker.jsx         # Horizontal scrolling news ticker with KZ pulse
│       │   └── AccountSetupModal.jsx  # Balance + risk% input overlay
│       └── pages/
│           ├── Dashboard.jsx     # Home: stats, signal preview, news ticker, module grid
│           ├── Academy.jsx       # Module list + module detail (AcademyHome + ModulePage)
│           ├── Lesson.jsx        # Lesson content + reading progress + quiz engine
│           ├── Signals.jsx       # Professional ICC scanner, Kill Zone clock, news shields
│           ├── Journal.jsx       # Trade log (CRUD, stats: WR, PF, expectancy)
│           ├── Corrector.jsx     # Trade grader — 5 ICC rules, A/B/C/F grade
│           ├── Tools.jsx         # Pip calc, position sizer, R:R calc, session clock, breakeven
│           └── Assistant.jsx     # AI chat → /api/chat → Claude API
│
└── backend/
    ├── main.py                   # FastAPI app, WebSocket, all routes, lifespan tasks
    ├── scanner.py                # Background ICC scanner (6-factor scoring, every 2 min)
    ├── tradingview.py            # CDP connector (tab discovery, JS eval, OHLCV fetch)
    ├── assistant.py              # Claude API chat with live context injection
    ├── news_fetcher.py           # ForexLive + FXStreet RSS, every 60s, impact detection
    ├── calendar_fetcher.py       # ForexFactory JSON calendar, every 30min, risk-by-pair
    ├── requirements.txt          # fastapi, uvicorn, anthropic, httpx, beautifulsoup4, aiohttp, python-dotenv
    ├── render.yaml               # Render.com deployment config
    ├── .env                      # ANTHROPIC_API_KEY
    └── .env.example
```

---

## 4. Routes

| URL | Component | Description |
|---|---|---|
| `/` | `Dashboard` | Home: stats, news ticker, signal preview, module grid |
| `/academy` | `AcademyHome` | All 9 modules with lock/progress |
| `/academy/module/:moduleId` | `ModulePage` | Lessons list for a module |
| `/academy/module/:moduleId/lesson/:lessonId` | `LessonPage` | Lesson content + quiz |
| `/signals` | `Signals` | Live scanner + Kill Zone clock + news shields |
| `/journal` | `Journal` | Trade log |
| `/corrector` | `Corrector` | Trade grader |
| `/tools` | `Tools` | Calculators + account balance settings |
| `/assistant` | `Assistant` | AI chat |

---

## 5. API Endpoints

| Method | Path | Description |
|---|---|---|
| GET | `/api/health` | Server status, TradingView connection, Anthropic key |
| GET | `/api/signals` | Latest cached ICC signal scan (6-factor scored) |
| POST | `/api/chat` | `{ message, history[] }` → `{ reply }` from Claude |
| POST | `/api/feed` | Push OHLCV bars from MCP into scanner cache |
| GET | `/api/feed/:symbol` | Check what's cached (includes timeframe param) |
| GET | `/api/news` | Latest news items (limit param, default 50) |
| GET | `/api/calendar` | Full calendar: upcoming events, next HIGH event, risk by pair |
| WS | `/ws/signals` | Live push: signals, alerts, KZ open, news_update |

---

## 6. The Trading Methodology

### Pairs Traded
| Pair | Role |
|---|---|
| XAUUSD | Primary (high-volatility, USD-driven) |
| EURUSD | Primary |
| GBPUSD | Primary |
| NZDJPY | Secondary |

Scanner also supports: GBPJPY, USDJPY, AUDUSD, USDCAD, USDCHF

### Timeframes
- **H1** — signal detection and EMA50 trend filter
- **M15** — Order Block and FVG refinement  
- **M5** — precise execution

### Kill Zones (UTC = Morocco −1h)
| Zone | UTC | Morocco | Notes |
|---|---|---|---|
| London KZ | 09:00–12:00 UTC | 10:00–13:00 | Primary. Best for all pairs. |
| NY KZ | 14:30–17:30 UTC | 15:30–18:30 | Strong setups, especially post-London continuation. |

**Rule: No trades outside Kill Zones. Ever.**

### The ICC Method (Indication / Correction / Continuation)

All 5 conditions must be met:

```
1. KILL ZONE TIMING
   Trade must be during London or NY KZ.

2. TREND ALIGNMENT (EMA50 on H1)
   Long trade → price ABOVE 50 EMA on H1
   Short trade → price BELOW 50 EMA on H1

3. BASE CANDLE (The "Indication")
   Body/range ratio ≥ 60%. Trend-aligned. Breaking recent swing.
   This is the institutional displacement candle.

4. CORRECTION (Pullback)
   Price retraces ≥ 30% back into the base candle zone.
   Wait for this — never enter on the base candle itself.

5. CONTINUATION (Entry)
   Price closes beyond base candle high (long) or low (short).
   Entry: at the continuation close price.
   Stop Loss: below/above base candle + 2-pip buffer.
   Take Profit: 3:1 R:R minimum → next BSL/SSL pool.
```

### The Signal Scoring System (0–100 points) — v2
| Factor | Points | Notes |
|---|---|---|
| Kill Zone active | +25 | London 09-12 UTC / NY 14:30-17:30 UTC |
| Valid Order Block (OB) | +20 | Prefers M15; H1+M15 confluence noted |
| Fair Value Gap (FVG) | +15 | Pair-correct pip size (fixed v1 bug) |
| Market Structure (BoS or CHoCH) | +15 | From swing high/low analysis |
| EMA50 trend aligned | +10 | Always scores (sets direction) |
| Premium / Discount zone | +10 | Longs in discount (<50%), shorts in premium (>50%) |
| No recent high-impact news | +5 | 20-min window |

**Grade thresholds:** STRONG ≥80 · WATCH 60–79 · MONITORING <60

**Signal also includes:** `confluence_count` (0-7), `premium_discount` (zone, pct, correct), `atr`, `liq_targets` (BSL/SSL swing levels), `rr1/rr2/rr3` (actual R:R from swing targets)

### ICC Concepts Used
| Concept | Usage |
|---|---|
| BSL / SSL | TP targets — price delivers to opposing liquidity |
| IPDA 20-day | Macro TP targeting (20-day high/low as next delivery zone) |
| Order Blocks | Last opposing candle before impulse → institutional zone |
| Fair Value Gaps (FVG) | Price imbalance → return-to-fill target |
| Premium / Discount | Enter discount for longs, premium for shorts |
| Market Structure (HH/HL, LH/LL) | Trend confirmation + bias |
| BoS / CHoCH | Trend continuation vs warning of reversal |

### Risk Rules
| Rule | Value |
|---|---|
| Max risk per trade | 1–2% of account |
| Minimum R:R | 3:1 |
| Target R:R | 5:1 |
| Daily loss limit | Stop after 2 consecutive losses |
| Weekly drawdown limit | Stop after 3% |

---

## 7. The Course Curriculum

9 modules, ~20 lessons total (modules 3–9 have 1–2 lessons each).

### Content Block Types (in `curriculum.js`)
| Type | Description |
|---|---|
| `text` | Plain paragraph |
| `subheading` | Section title within lesson |
| `highlight` | Gold-bordered key insight box |
| `concept` | Blue concept callout box |
| `formula` | Mono-font formula block |
| `example` | Practical example with real numbers |
| `warning` | Red warning box |
| `table` | Data table with headers + rows |
| `diagram` | Renders an SVG from `Diagrams/index.jsx` by `id` |

### Diagram IDs (all implemented)
| ID | Lesson | Description |
|---|---|---|
| `candlestick` | 2-1 | Bull/bear candle anatomy with labels |
| `market-structure` | 3-1 | Uptrend HH/HL sequence |
| `bos-choch` | 3-1 | Break of Structure vs Change of Character split view |
| `liquidity` | 4-1 | BSL/SSL pools + stop hunt visualization |
| `order-block` | 5-1 | Bullish + bearish OB identification |
| `fvg` | 5-2 | 3-candle Fair Value Gap with return-to-fill |
| `premium-discount` | 5-3 | Premium/discount zones with EQ line |
| `icc` | 6-1 | Full ICC setup: Indication → Correction → Continuation |

### Module Summary
| # | Title | Lessons | Key Diagrams |
|---|---|---|---|
| 1 | How Markets Really Work | 3 | — |
| 2 | Reading the Chart | 2 | candlestick |
| 3 | Market Structure | 1 | market-structure, bos-choch |
| 4 | Liquidity — The Real Engine | 2 | liquidity |
| 5 | ICC Toolkit | 3 | order-block, fvg, premium-discount |
| 6 | The ICC Method ⭐ | 2 | icc |
| 7 | Risk Management | 1 | — |
| 8 | Trading Psychology | 1 | — |
| 9 | Your Trading System | 1 | — |

### Progress Rules
- Completing a lesson grants **+100 XP**
- Each correct quiz answer grants **+50 XP**
- Module unlock requires **≥50% completion** of the previous module
- Streak increments when studying on consecutive days

---

## 8. The Signal Scanner

**File:** `backend/scanner.py`

- Runs every **2 minutes** as a background `asyncio` task
- Pairs: XAUUSD, EURUSD, GBPUSD, NZDJPY
- Timeframes: H1 (trend + OB) + M15 (refined OB + FVG)

### Scoring Logic
1. Kill Zone check (UTC times)
2. EMA50 calculated from last 50 H1 closes
3. Order Block: last opposing candle before impulse, price inside zone
4. FVG: 3-candle gap ≥ 3 pips, price at/inside
5. Market Structure: swing high/low analysis with BoS/CHoCH detection
6. News check: `get_cached_news()` from news_fetcher — deducts 10pts if HIGH news in last 30min

### Signal Format
```python
{
  pair, direction, timeframe, score, grade,
  criteria: { kill_zone, order_block, fvg, market_structure, ema50, news_clear },
  entry, sl, tp1, tp2, tp3,
  pip_sl, pip_tp1, pip_tp2, pip_tp3,
  ob_zone, fvg_zone, ema50,
  timestamp, expires_at  # 4 hours from detection
}
```

### OHLCV Data Flow
```
MCP tools → POST /api/feed → _ohlcv_cache["{SYMBOL}_{TF}"]
                                        ↓
                               scanner._get_ohlcv(pair, tf)
                                        ↓
                          CDP direct fetch (fallback)
```

---

## 9. Real-Time News System

**File:** `backend/news_fetcher.py`

- Sources: ForexLive (`/feed/`) + FXStreet (`/rss/news`)
- Polled every **60 seconds** via `httpx`
- Impact classification: HIGH (30+ keywords: cpi, nfp, fed, fomc, powell, gdp...) / MEDIUM / LOW
- Currency detection: `currencies` field is a list, e.g. `["USD", "GBP"]`
- Deduplication by MD5 hash of `source:title`
- Broadcasts `{ news_update: [...] }` via WebSocket on new items

**File:** `backend/calendar_fetcher.py`

- Source: ForexFactory JSON API (this week + next week)
- Refreshes every **30 minutes**
- `get_news_risk_for_pair(pair, window_minutes=30)` → `{ level, events, next_event }`
  - Checks upcoming AND past 5 min events
- `get_next_high_event()` → next global HIGH event with `minutes_until`
- `get_cached_calendar()` → full dict with `upcoming_30m`, `upcoming_60m`, `next_high`, `today`, `risk_by_pair`

**Endpoint:** `GET /api/calendar`

---

## 10. News Shield System (Signal Cards)

**File:** `src/components/NewsShield.jsx`

Inline badge on each signal card in Signals.jsx:
- 🛡 Clear (green) — no HIGH/MEDIUM news within 30min
- ● MED in Xm (gold, steady) — medium impact news nearby
- ⚠ HIGH in Xm (red, pulsing) — high impact news nearby

**File:** `src/hooks/useCalendar.js`

Polls `/api/calendar` every 60s. Exposes:
- `getNewsRiskForPair(pair)` → `{ level, events, next_event }`
- `nextHighEvent` → next global HIGH event (for ticker countdown)

**Signal card override behavior:**
- STRONG signal + HIGH news → red border, red background, `⛔ HIGH IMPACT NEWS — WAIT · [event title]` banner above card

---

## 11. Alert & Notification System

**File:** `src/contexts/AlertContext.jsx`

Single WebSocket connection at `ws://hostname:8000/ws/signals`. Shared across all pages.

### Toast Types
| Type | Sound | Duration | Trigger |
|---|---|---|---|
| `signal` | 3 beeps | 12s | STRONG signal detected |
| `killzone` | ascending chime | 7s | Kill Zone opens |
| `confluence` | 3 beeps | 7s | HIGH news + Kill Zone simultaneously |
| `warning` | 3 beeps | 7s | HIGH news + open journal trades OR STRONG signal |

### Currency Matching (AlertContext)
When HIGH news drops via `news_update`, the system:
1. Checks `PAIR_CURRENCIES` map to find which pairs are affected
2. Compares against current STRONG signals (`signalsRef.current`)
3. Fires: "⛔ News Risk — {pair} STRONG signal" toast + native notification
4. Checks open journal trades (`trading_journal` localStorage) for pair match
5. Fires: "⚠ High news — X open trades at risk" with pair names

### NewsTicker (Dashboard + Signals)
- Scrolls top 5 news headlines
- `nextHighEvent` prop → pinned countdown: "⚡ NFP in 14m"
- `news-ticker-pulse-gold` class: slow gold glow when KZ active + HIGH news in feed
- `news-ticker-flash-red` class: red flicker 3s when new HIGH news arrives

---

## 12. localStorage Keys

| Key | Owner | Content |
|---|---|---|
| `trading_journal` | Journal.jsx | Array of trade objects |
| `trading_academy_progress` | useProgress.js | `{ completedLessons, quizScores, totalXP, streak, lastStudied }` |
| `ta_account_balance` | useAccount.js | Number (account balance in USD) |
| `ta_account_risk_pct` | useAccount.js | Number (risk % per trade) |
| `ta_price_alerts` | Signals.jsx | `{ pair: { entry, signal, set_at } }` |

---

## 13. Position Sizing

**File:** `src/hooks/useAccount.js`

```js
// Pip sizes
XAUUSD: 0.10, JPY pairs: 0.01, others: 0.0001

// Pip value per standard lot (USD-denominated)
EURUSD/GBPUSD: $10, XAUUSD: $100, NZDJPY: $6.67

// Formula
lots = (balance × riskPct/100) / (pipRisk × pipValuePerLot)
```

Signal cards show: lots, riskUSD, profitTP1 (+1R), profitTP2 (+2R), profitTP3 (+3R)

---

## 14. The Trade Corrector

**File:** `frontend/src/pages/Corrector.jsx`

Grades any trade against 5 ICC rules. A=5/5, B=4/5, C=3/5, F≤2/5.

| # | Rule | Pass Condition |
|---|---|---|
| 1 | Kill Zone timing | During London or NY KZ |
| 2 | Trend alignment | Direction matches EMA50 H1 |
| 3 | Base candle quality | Body ratio ≥ 60% |
| 4 | Correction before entry | Waited for ≥30% pullback |
| 5 | R:R ratio | ≥ 3:1 |

---

## 15. The Trade Journal

**File:** `frontend/src/pages/Journal.jsx`

- `localStorage` key: `trading_journal`
- All calculation is **client-side only** — no backend required

### Pip & P&L Calculation
```js
PIP_SIZE  = { XAUUSD: 0.1, GBPJPY: 0.01, USDJPY: 0.01, NZDJPY: 0.01, default: 0.0001 }
PIP_VALUE = { XAUUSD: 100, GBPJPY: 6.67, NZDJPY: 6.67, default: 10 }  // per standard lot

pips = (exit - entry) / pipSize  × direction_sign
pnl  = pips × pipValue × lotSize
rr   = reward_pips / risk_pips
```

### ICC Grading (A/B/C/F)
4 criteria, 1 point each:
1. Kill Zone (checkbox)
2. Trend Aligned (checkbox)
3. ICC Valid (checkbox)
4. R:R ≥ 3 (auto-computed from entry/sl/tp)

Score → A=4, B=3, C=2, F≤1

### Trade Object Schema
```js
{
  id, date, pair, direction, session,       // meta
  entry, sl, tp, exitPrice, lotSize,        // prices
  pips, pnl,                                // auto-computed
  rr, grade,                                // auto-computed
  kz, trendAligned, iccValid,               // ICC checkboxes
  result, notes, status,                    // outcome ('open'|'win'|'loss'|'be')
  tp1, tp2, signalScore, signalGrade        // from signal card (Paper Trade)
}
```

### UI Features
- **Equity Curve** — SVG cumulative pips chart (polyline + area fill, zero baseline, trade dots)
- **TradeForm** — real-time auto-compute of pips/pnl/rr/grade as user types; ICC checkboxes; grade preview badge
- **TradeCard** — expandable card with grade ring (A/B/C/F color-coded), session tag, pips + dollar display, ICC checklist in expanded view
- **WeekGroup** — weekly header with W/L count, total pips + P&L for that week
- **GradeBar** — horizontal A/B/C/F distribution bar
- **Open Positions** — shown at top of page with pulsing green dot, separate from closed trades
- **Stats** — Win Rate, Total P&L ($), Profit Factor, Expectancy (pips/trade), avg R:R, grade distribution
- **Filters** — pair filter (dynamic from actual trades) + ALL/WIN/LOSS/BREAKEVEN result tabs

---

## 16. The AI Assistant

**File:** `backend/assistant.py`

- Model: `claude-sonnet-4-6`
- System prompt: full ICC methodology, pairs, rules, ICC concepts, grading criteria
- Live context injected per request: UTC time, Kill Zone status, live price if chart-related
- Keeps last 10 messages of history
- Requires `ANTHROPIC_API_KEY` in `backend/.env`

---

## 17. Design System

**Theme:** Deep dark (`--bg: #07070d`) with purple-tinted surfaces  
**Accent:** Gold (`#f5a623`) — buttons, active states, prices, progress  
**Success:** Cyan-green (`#22d3a5`) — wins, long signals, completed modules  
**Danger:** Soft red (`#f87171`) — losses, short signals, warnings  
**Fonts:** Inter (UI) + JetBrains Mono (prices, formulas, code)

All design tokens and component classes in `frontend/src/index.css`.

Key component classes: `.card`, `.card-gold`, `.stat-card`, `.btn`, `.btn-primary`, `.tag-*`, `.signal-card`, `.quiz-*`, `.lesson-*`, `.news-shield-*`, `.news-ticker-*`, `.confluence-banner`

---

## 18. How to Run

```bash
cd ~/trading-academy
./start.sh
```

- Frontend: http://localhost:5173
- Backend API: http://localhost:8000/api/health

**Requirements:**
- Node.js v18+ (install via `nvm`)
- Python 3.11+
- `ANTHROPIC_API_KEY` in `backend/.env`
- TradingView Desktop open for live signals (CDP port 9222)

**Push OHLCV data to scanner (from MCP context):**
```
1. chart_set_symbol("XAUUSD") → chart_set_timeframe("60") → data_get_ohlcv(count=120)
2. POST /api/feed with { symbol: "XAUUSD", timeframe: "60", bars: [...] }
Repeat for M15 (timeframe: "15") and for each pair.
```

---

## 19. Current Status (2026-04-18)

### ✅ Complete and Working

**Infrastructure**
- Full-stack app running on localhost (React + FastAPI)
- WebSocket live push for signals, news, Kill Zone opens
- Service worker registered (future Web Push)
- Mobile-responsive layout: bottom nav bar, hamburger sidebar, touch targets

**Signal Scanner (Step 5 — fully upgraded)**
- 7-criterion scoring (0–100): KZ(25) + OB(20) + FVG(15) + Structure(15) + EMA50(10) + P/D(10) + News(5)
- ATR-based SL placement (replaces fixed 2-pip offset — critical for XAUUSD)
- Swing-level TP targets: BSL (longs) / SSL (shorts) from actual swing highs/lows
- Multi-TF OB confluence detection: H1+M15 bonus label when both confirm
- Fixed FVG pip-size bug (was always 0.0001, now pair-specific)
- Signal cards: confluence dots (7 circles), Premium/Discount pill, ATR display, real R:R on TPs
- STRONG/WATCH/MONITORING tiers with color coding
- Position sizing from account balance (lots, riskUSD, profitTP1/2/3)
- Paper Trade → writes to Journal, Set Alert → writes to localStorage
- Signal expiry countdown (4-hour window)

**News System**
- ForexLive + FXStreet RSS feeds, polled every 60s
- Impact classification (HIGH/MEDIUM/LOW)
- Currency auto-detection from headlines
- ForexFactory economic calendar (this week + next week), refreshed every 30min
- News risk by pair: `{ level: 'HIGH'|'MEDIUM'|'NONE', events, next_event }`

**News Shield (Signal Cards)**
- Green/gold/red shield badge on every signal card
- `⛔ HIGH IMPACT NEWS — WAIT` banner for STRONG signals with HIGH news
- Currency-matched alerts: HIGH news → toast for every affected STRONG signal
- Open trade risk: HIGH news + matching open journal trade → warning toast + notification

**NewsTicker (Dashboard)**
- Scrolling top-5 headlines with impact color coding
- Countdown badge: "⚡ NFP in 14m" when next HIGH event is known
- Pulse gold animation when Kill Zone + HIGH news are active simultaneously
- Red flash (3s) when new HIGH news arrives

**Kill Zone System**
- Real-time Morocco clock on Signals page
- Kill Zone open broadcaster via WebSocket
- Alert toast + native notification when KZ opens
- HIGH CONFLUENCE alert when KZ + HIGH news coincide

**Academy**
- 9 modules, 20 lessons, full content + quizzes
- 8 SVG diagrams covering all major concepts
- Reading progress bar, XP animations, keyboard shortcuts in quiz
- Module unlock gates (≥50% previous module)
- Progress tracking: completedLessons, quizScores, XP, streak

**Tools**
- Pip calculator, position sizer, R:R calculator, session clock, breakeven calculator
- Account balance setup (persisted, used by signal cards for live sizing)

**Journal (Step 4 — fully upgraded)**
- Auto pip + dollar P&L calculation from entry/exit/lot size + pair-specific pip values
- ICC grading (A/B/C/F) auto-computed from checkboxes + R:R
- Equity curve SVG: cumulative pips over time with area fill
- Weekly grouping with W/L tallies and weekly pip/P&L totals
- Open positions section with pulsing indicator
- Stats: Win Rate, Total P&L ($), Profit Factor, Expectancy, avg R:R, grade distribution
- Expandable trade cards with full ICC checklist
- Pair filter + result tabs (ALL/WIN/LOSS/BREAKEVEN)
- Paper trades from signal cards auto-populate all fields

**Trade Corrector**
- 5-rule ICC grader, A/B/C/F grade output

**AI Assistant**
- Claude API chat with live trading context injection

### ❌ Not Yet Built
- **Real-time OHLCV auto-refresh** — scanner still relies on manually pushed bars via `/api/feed`
- **Web Push Notifications (mobile)** — requires HTTPS deployment (now possible post-deploy)
- **Screenshot attachment in Journal** — not yet implemented
- **WhatsApp / Telegram alerts** — not yet integrated
- **Automated MCP → feed push** — no cron yet; user must push manually

---

## 20. What to Do Next (Priority Order)

1. **PWA manifest** — `manifest.json` + icon set → "Add to Home Screen" on iPhone
2. **Auto OHLCV refresh** — wire a scheduler inside scanner to re-fetch from MCP/CDP
3. **Telegram/WhatsApp alerts** — Telegram bot is simplest; listen to scanner broadcasts
4. **Backtesting view** — replay historical setups against the 6-factor criteria
5. **Multi-pair dashboard tile** — show all 4 pairs + their news risk + current score at a glance
6. **Journal screenshot attachment** — drag-and-drop or paste image per trade, stored as base64 in localStorage
